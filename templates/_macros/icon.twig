{% macro icon(args) %}
    {% set args = {
        class: '',
        name: ''
    } | merge(args) %}

    {% if args.name %}
        {{ svg('@templates/_svgs/icons/' ~ args.name ~ '.svg') | attr({ class: args.class })}}
    {% endif %}
{% endmacro %}
