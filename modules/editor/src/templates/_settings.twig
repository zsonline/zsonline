{% import '_includes/forms' as forms %}

{{ forms.checkboxSelectField({
    id: 'embedVolumes',
    name: 'embedVolumes',
    label: 'Embed Volumes'|t('editor'),
    instructions: 'The volumes that should be available when selecting embeds.'|t('editor'),
    options: availableEmbedVolumes,
    values: field.embedVolumes,
    showAllOption: availableEmbedVolumes|length ? true : false
}) }}

{{ forms.checkboxSelectField({
    id: 'fileVolumes',
    name: 'fileVolumes',
    label: 'File Volumes'|t('editor'),
    instructions: 'The volumes that should be available when selecting files.'|t('editor'),
    options: availableFileVolumes,
    values: field.fileVolumes,
    showAllOption: availableFileVolumes|length ? true : false
}) }}

{{ forms.checkboxSelectField({
    id: 'imageVolumes',
    name: 'imageVolumes',
    label: 'Image Volumes'|t('editor'),
    instructions: 'The volumes that should be available when selecting images.'|t('editor'),
    options: availableImageVolumes,
    values: field.imageVolumes,
    showAllOption: availableImageVolumes|length ? true : false
}) }}

{{ forms.editableTableField({
    label: 'Templates'|t('editor'),
    instructions: 'Choose the template with which a block should be rendered.'|t('editor'),
    id: 'templates',
    name: 'templates',
    cols: {
        label: {
            type: 'heading',
            heading: 'Block'|t('editor'),
        },
        node: {
            type: 'singleline',
            class: 'hidden'
        },
        template: {
            type: 'template',
            heading: 'Template'|t('editor'),
            info: 'Which template should be used for rendering the block.'|t('editor'),
            code: true,
            disable: true
        },
    },
    rows: templates,
    errors: field.getErrors('templates')
}) }}
